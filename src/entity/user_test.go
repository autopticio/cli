package entity

import (
	"testing"
)

func TestUserSignIn(t *testing.T) {

	/*user := User{
		Name:     "demo.sre",
		Email:    "demo.sre@autoptic.com",
		Password: "d3m02024ai",
		Role:     "user",
	}

	signIn, err := signIn("https://demo.autoptic.com", user)
	log.Printf("signIn: %v", signIn)
	assert.NoError(t, err)
	assert.NotNil(t, signIn)*/
}
